# The following lines of boilerplate have to be in your project's top-level
# CMakeLists.txt in order to build the app.
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(esp32s3_l298n_demo)
